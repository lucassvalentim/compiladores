fn main(){ 
    let a, c: float;
    a = 3 + a + c * 1;
    d = 100 * 2 + 1 / 4;

    if a < c * 1 + a - 1 {
        if (2 < 3) ==  (a + 1 != d) {
            c = 1;
            return a;
        }

        return a + c;
    }
}